# HiddenReps 로드맵

기획자·개발자·디자이너 3인 관점으로 2026-08-22에 진행한 앱 진단을 바탕으로 정리한 순차 실행 로드맵. 아티팩트(claude.ai)로도 한 번 보여줬지만, 그건 열람용 산출물일 뿐이고 이 파일이 원본/추적 대상이다.

## 현재 상태 한 줄 요약

측정 도구로는 탄탄하다(운동 커스터마이징, 4개 언어, 다크/라이트 테마, 온보딩, 백업 export/import 등은 이미 견고). 하지만 **다시 켜고 싶게 만드는 장치**(알림·스트릭·목표·통계 그래프)가 전무하고, 개발 쪽엔 자동 테스트·에러 리포팅이 없어 데이터 유실 리스크에 안전망이 없다.

## 관점 1 — 기획 (PM)

**부족한 점**

1. 리텐션 장치 전무 — 알림·스트릭·"오늘 아직 안 했어요" 넛지 없음
2. 진척이 눈에 안 보임 — 캘린더/히트맵, 이번 주 요약 없음
3. 목표 설정 불가
4. 공유·바이럴 장치 없음 (기록 카드 공유, 마일스톤 배지)
5. 수익모델이 배너광고 하나뿐
6. 기기 이전 문턱 — 수동 파일 export/import뿐, 자동 클라우드 백업 없음

**추가해야 할 것 (우선순위 순)**

1. [Phase 1] 세트 간 휴식 타이머 (횟수형 운동에도)
2. [Phase 2] 로컬 알림·리마인더
3. [Phase 2] 홈 화면 "이번 주 요약" + 캘린더 히트맵 + 스트릭
4. [Phase 2] 목표 설정·달성률
5. [Phase 3] 기록 카드 이미지 공유
6. [Phase 3] 프리미엄 구독(광고 제거 + 고급 통계 + 자동 백업)
7. [Phase 4] Apple Health / Google Fit 연동

## 관점 2 — 개발 (Dev)

**부족한 점**

1. 자동 테스트 0건 — 타이머·저장 큐·백업 복원처럼 데이터 유실 직결 코드에 회귀 방지 장치 없음
2. `storage.ts`의 `readJson`이 파싱 실패 시 조용히 fallback — 손상된 데이터를 사용자에게 알리지 않음
3. 에러 리포팅 도구 없음 — 프로덕션 크래시를 알 방법이 없음
4. 기록이 하나의 JSON 배열로 저장돼 누적될수록 느려질 구조
5. 백업 스키마 버전은 있지만 마이그레이션 함수 부재
6. CI 파이프라인 없음 — 타입체크·린트가 로컬 실행 의존

**추가해야 할 것 (우선순위 순)**

1. [Phase 1] `storage.ts` 핵심 함수 스모크 테스트
2. [Phase 1] 에러 리포팅 도입(Sentry 등)
3. [Phase 1] GitHub Actions CI(타입체크·린트)
4. [Phase 2] `readJson` 파싱 실패 시 사용자 알림 경로 추가
5. [Phase 3] 백업 스키마 마이그레이션 전략 문서화·구현
6. [Phase 4] AsyncStorage → expo-sqlite 전환 검토

## 관점 3 — 디자인 (Design)

**부족한 점**

1. 기록 화면이 텍스트뿐 — 그래프·스파크라인 없음
2. 세트 간 휴식 타이머 없음 (횟수형 운동)
3. PR 갱신 순간이 밋밋함 — 토스트 하나뿐
4. 홈 화면 정렬·재배열 불가
5. 로딩 상태가 빈 화면(스켈레톤 없음)
6. 태블릿·가로모드 레이아웃 미고려

**추가해야 할 것 (우선순위 순)**

1. [Phase 1] 횟수형 운동에도 세트 간 휴식 타이머
2. [Phase 1] 로딩 스켈레톤 UI
3. [Phase 2] 기록 화면 추이 스파크라인
4. [Phase 2] PR 갱신 축하 마이크로 인터랙션
5. [Phase 2] 홈 화면 "이번 주 요약" 카드 + 드래그 정렬
6. [Phase 4] 홈스크린 위젯(오늘 상태·스트릭)
7. [Phase 4] 태블릿 레이아웃 최적화

## 통합 로드맵

### Phase 1 — 기반 다지기 (즉시 착수)

리스크를 낮추고 가장 저비용으로 체감 만족도를 올린다.

- [x] 세트 간 휴식 타이머 (기획·디자인) — 완료 (2026-08-23 커밋 `b4b8653`)
- [x] storage.ts 핵심 함수 스모크 테스트 (개발) — 완료 (2026-08-23)
- [ ] 에러 리포팅 도입 (개발)
- [x] CI 자동화 (개발) — 완료 (2026-08-23, `.github/workflows/exercise-diary-ci.yml`)
- [x] 로딩 스켈레톤 UI (디자인) — 완료 (2026-08-23, 홈 화면 운동 목록)

### Phase 2 — 리텐션 코어

"다시 켜고 싶은 앱"으로 — 습관 형성 장치를 넣는다.

- [x] 로컬 알림·리마인더 (기획) — 완료 (2026-08-23)
- [x] 이번 주 요약 + 캘린더 히트맵 + 스트릭 (기획·디자인) — 완료 (2026-08-23, 커밋 `2659320`). 히트맵은 실기기 확인 중 "무슨 의미인지 모르겠다"는 피드백으로 일반 월간 캘린더로 교체됨
- [ ] 목표 설정·달성률 (기획)
- [ ] 기록 추이 스파크라인 (디자인)
- [ ] PR 축하 인터랙션 (디자인)
- [ ] readJson 무음 실패 제거 (개발)

### Phase 3 — 성장·수익화

확보한 사용자를 자산화한다.

- [ ] 기록 카드 이미지 공유 (기획)
- [ ] 프리미엄 구독 (기획)
- [ ] 백업 스키마 마이그레이션 전략 (개발)

### Phase 4 — 생태계 확장

앱을 기기·플랫폼 경계 밖으로 확장한다.

- [ ] Apple Health / Google Fit 연동 (기획)
- [ ] 홈스크린 위젯 (디자인)
- [ ] 태블릿 레이아웃 최적화 (디자인)
- [ ] AsyncStorage → SQLite 전환 (개발)

## 진행 기록

- 2026-08-22: 로드맵 초안 작성. Phase 1 "세트 간 휴식 타이머" 스펙(`docs/specs/세트 간 휴식 타이머.md`)·설계(`docs/design/세트 간 휴식 타이머.md`) 완료, 구현 대기.
- 2026-08-23: "세트 간 휴식 타이머" 구현·QA·코드 리뷰 완료, 커밋 `b4b8653`. QA 중 시간형 운동 측정 후 오디오가 앱 전역에서 잠기는 기존 결함을 발견해 함께 수정. Phase 1 다음 항목은 storage.ts 스모크 테스트 / 에러 리포팅 / CI 자동화 / 로딩 스켈레톤 UI.
- 2026-08-23: `jest`+`jest-expo` 도입, `storage.ts`의 운동/기록 CRUD·쓰기 큐 직렬화·설정 기본값 병합·백업 복원 로직에 유닛 테스트 8개 추가(`npm test`). Phase 1 다음 항목은 에러 리포팅 / CI 자동화 / 로딩 스켈레톤 UI.
- 2026-08-23: GitHub Actions CI 추가(`.github/workflows/exercise-diary-ci.yml`, RNProject 루트 — 여러 프로젝트가 함께 있는 저장소라 `Exercise_Diary/**` 변경일 때만 실행). tsc·`npm test`는 필수 게이트, lint는 기존 위반 20건이 우선순위 낮게 남아있어 비차단(참고용)으로만 돈다. Phase 1 마지막 항목은 로딩 스켈레톤 UI.
- 2026-08-23: 재사용 가능한 `Skeleton` 컴포넌트(모션 감소 설정 존중, 은은한 깜빡임) 도입, 홈 화면 운동 목록이 AsyncStorage 로드 전 빈 화면 대신 카드 3장 모양의 스켈레톤을 보여주도록 함. 임시로 로딩을 지연시켜 실기기에서 육안 확인 후 되돌림. **Phase 1 전체 완료.** 다음은 Phase 2(리텐션 코어) — 로컬 알림·리마인더 / 이번 주 요약+캘린더 히트맵+스트릭 / 목표 설정 / 기록 추이 스파크라인 / PR 축하 인터랙션 / readJson 무음 실패 제거.
- 2026-08-23: Phase 2 "로컬 알림·리마인더" 스펙·설계·구현·QA·코드 리뷰 완료. 사용자 요청으로 알림 방식을 매일 정해진 시각 / N일간 미기록 중 설정에서 선택하게 하고, 시각 선택은 네이티브 OS 피커(`@react-native-community/datetimepicker`)를 쓰도록 설계 — `DESIGN.md`에 "시스템 시간/날짜 피커는 예외적으로 네이티브 그대로 쓸 수 있다"는 예외 규칙을 추가했다. QA·코드 리뷰 과정에서 발견해 함께 고친 문제 셋: (1) 권한 거부 안내를 전용 `<Modal>`로 만들었다가 iOS의 Modal-on-Modal 조용한 실패 버그를 발견해 기존 `showAlert()`(embedded `AlertHost`) 패턴으로 교체, (2) 설정 변경 재예약이 직렬화되지 않아 실패 시 이후 저장이 조용히 무시될 수 있던 문제를 큐로 고침, (3) N일간 미기록 모드가 1회성 알림이라 발송 후 재무장이 안 되던 문제를 앱 콜드 스타트 시 재계산(`resyncReminderSchedule`)으로 완화(완전한 해결은 백그라운드 작업이 필요해 후속 과제로 남음). Phase 2 다음 항목은 이번 주 요약+캘린더 히트맵+스트릭 / 목표 설정·달성률 / 기록 추이 스파크라인 / PR 축하 인터랙션 / readJson 무음 실패 제거.
- 2026-08-23: Phase 2 "이번 주 요약 + 캘린더 히트맵 + 스트릭" 스펙·설계·구현·QA·코드 리뷰 완료, 커밋 `2659320`. 구현 도중 실기기 확인 과정에서 세 차례 방향 전환이 있었다: (1) 원래 히트맵을 홈 화면 요약 카드 안에 넣는 설계였으나, 히트맵이 실제 높이를 갖게 되자 홈 화면 운동 목록을 밀어내고 FAB과 겹쳐 별도의 "활동 기록" 화면(요약 카드를 탭해 이동)으로 분리, (2) 그 8주 히트맵을 실기기로 두 차례 확인한 뒤 "무슨 의미인지 모르겠다"는 피드백을 받아 범례·요일 라벨을 더해도 불명확해 누구나 바로 읽는 일반 월간 캘린더(이전/다음 달, 최근 3년 연도·월 직접 선택)로 완전히 교체, (3) 캘린더 날짜를 눌러 그날 기록한 운동 목록을 보여주고 그 운동의 "기록" 탭으로 바로 이동하는 인터랙션을 추가 요청받아 구현(`ExerciseScreen`에 `initialTab` prop 추가). 스트릭 배지는 운동별 개별 계산으로 홈 운동 카드에 유지. `stats.ts`에 히트맵→캘린더 전환으로 남은 레이스 컨디션·중복 AsyncStorage 조회, 홈 화면 빈 상태 레이아웃 회귀(헤더까지 중앙 쏠림), `totalReps`의 components→lib 계층 위반 등을 코드 리뷰에서 찾아 함께 고쳤다. Phase 2 다음 항목은 목표 설정·달성률 / 기록 추이 스파크라인 / PR 축하 인터랙션 / readJson 무음 실패 제거.
